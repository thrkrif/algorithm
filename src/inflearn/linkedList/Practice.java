package inflearn.linkedList;

public class Practice {
    /*
     * LinkedList의 등장 이유? 배열은 크기를 미리 정해둬야 한다.
     * 처음부터 크기를 정할 수 없다면? 동적으로 크기가 정해져야한다면? -> 링크드 리스트의 필요성
     * 동적으로 크기 변환이 가능하다.
     * 배열은 데이터 삽입, 삭제가 어려움. 삽입, 삭제할 때마다 인덱스를 이동시켜야함. 즉, 성능이 좋지 않음.
     *
     */
}
